# Railway Nixpacks config to include C++ standard libraries for NumPy/ChromaDB
# We DO NOT override the install phase here, so Railway will auto-detect pip and requirements.txt correctly.

[phases.setup]
nixPkgs = ["python312", "gcc", "stdenv.cc.cc.lib", "zlib"]
